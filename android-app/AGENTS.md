# Android agent rules

- Kotlin and Gradle Kotlin DSL unless the task records another decision.
- Do not store provider API keys in resources, BuildConfig or APK assets.
- Preserve local idle-video fallback.
- Treat USB microphone selection as runtime capability, not an assumption.
- Every UI state must have a visible and recoverable outcome.
- Do not claim device compatibility without real-device evidence.
