services:
  gateway:
    build:
      context: ../gateway
      dockerfile: Dockerfile
    container_name: jiyangjia-gateway
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
      - APP_NAME=jiyangjia-gateway
      - APP_VERSION=0.1.0
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=3).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - ../gateway/app:/app/app:ro
      - ../gateway/logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - gateway-network

networks:
  gateway-network:
    name: jiyangjia-gateway-network
