# TASK-015 Android 12 acceptance — NO-GO (2026-08-07)

## Decision

`NO-GO / BLOCKED`. The acceptance target is not present, so hardware-dependent claims cannot be made.

## Available local evidence

- Gateway: `python -m pytest tests -q` -> 63 passed, one FAISS/NumPy deprecation warning.
- Repository: `scripts/verify_repository.ps1` -> PASS.
- Android: `testDebugUnitTest lintDebug assembleDebug` -> BUILD SUCCESSFUL.
- Debug APK: `android-app/app/build/outputs/apk/debug/app-debug.apk`, `6451579` bytes, SHA-256 `79353D6547FF13A7415ACD00772F445E4084C301FAF3DC730859B7207627AFB5`.
- `adb devices -l` -> empty device list.
- Project-local SDK -> `EMULATOR_NOT_INSTALLED`; no Android 12 system image.

## Blocking gates

1. Android 12 physical display with firmware/ABI/DPI/resolution details.
2. USB microphone and target internal/external speaker.
3. Approved rights-cleared MP4/background and final Display Profile.
4. Trusted HTTPS bootstrap/APK hosting.
5. Formal production signing identity, two-custodian backup record and N/N+1/N+2 APKs.

## Untested and therefore failed acceptance gates

- USB/audio routing, AEC and 9/10 automatic barge-in latency.
- Visual Profile/pixel behavior and first-frame switching on target decoder.
- Offline/network recovery, process kill and 10 reboots.
- Device Owner, persistent Home and true Lock Task restrictions.
- Formal signed silent upgrade, rejection cases and N+2 rollback release.
- Eight-hour stability and 30 real consultations.

Resume TASK-015 without changing its acceptance thresholds once all five blocking inputs are available.
