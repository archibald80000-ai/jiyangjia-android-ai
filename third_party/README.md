# Third-party source checkouts

Run `scripts/bootstrap_livetalking.ps1` to clone the locked LiveTalking commit into `third_party/LiveTalking`. The checkout is ignored by this repository to avoid copying upstream history and model assets.
