# AGENTS.md — Codex / AI Agent 总执行规则

本文件适用于整个仓库。子目录中的 `AGENTS.md` 可以增加更严格的局部规则，但不得放宽本文件的安全和质量要求。

## 1. 先读后做

每个新会话必须先阅读：

1. `README.md`
2. `MEMORY.md`
3. `PROJECT_STATE.md`
4. `memory/CORE_FACTS.md`
5. `memory/DECISIONS.md`
6. `memory/HANDOFF.md`
7. 当前授权的 `tasks/TASK-xxx_*.md`
8. 将要修改目录内的局部 `AGENTS.md`

没有明确任务编号时，只允许审查、列出风险和建议下一任务；不得自行同时推进多个阶段。

## 2. 固定路径与只读资料源

- 本地项目根目录：`E:\work\安卓大屏AI语音客服系统`
- 原始业务资料：`E:\work\积养家`
- 原始业务资料默认只读：禁止删除、移动、覆盖、批量上传和未经批准的批量解析。
- `third_party/LiveTalking` 由脚本拉取，不把上游完整源码复制进本仓库提交历史。

## 3. 当前产品优先级

1. 可复现的 LiveTalking 官方能力；
2. Android 12 大屏可靠显示与音频输入输出；
3. 无 GPU 的 `idle_video` 降级闭环；
4. 模型 Adapter 与真实 API；
5. 小范围已确认 FAQ；
6. 正式知识库、后台和高级数字人能力。

不得为了“架构完整”提前引入 Dify、向量数据库、Kubernetes、本地大模型或大型管理后台。

## 4. 单任务原则

- 一次只执行一个 `TASK-xxx`。
- 开始前写出：目标、现状、准备修改文件、风险、验证命令。
- 只修改任务声明允许的范围。
- 遇到阻塞时先保存证据并完成可执行部分，不得伪造成功。
- 完成后必须更新：任务状态、`PROJECT_STATE.md`、`memory/CURRENT_STATE.md`、`memory/HANDOFF.md`、必要的决策记录。
- 不得把 Mock、占位、接口定义描述为真实接入完成。

## 5. Git 与发布规则

- `main` 应保持可审查、可回滚。
- 任务分支命名：`task/TASK-###-short-slug`。
- 提交信息：`TASK-###: 动词 + 结果`。
- 提交前检查 `git status` 和 diff，只暂存本任务文件；禁止 `git add .`、`git add -A`。
- 未获得明确授权时，不推送、不创建 PR、不合并、不发布 Release。
- 不在默认分支直接进行大范围开发。
- 上游 LiveTalking 的修改优先通过 Adapter、配置或小型补丁实现；所有补丁必须记录来源 commit 和目的。

## 6. 密钥与隐私

- 永远不读取或打印 `.env.local` 的值；只能报告变量名是否已配置。
- 禁止提交 API Key、Token、Access Key、Cookie、私钥、数据库密码和真实顾客录音。
- Android APK 不包含豆包或其他服务端密钥。
- 测试录音默认本地临时保存，测试结束删除；需要长期保留时必须得到明确授权。
- 公共仓库不得提交合同、价格底稿、个人身份信息、客户资料或未获授权的形象/声音素材。

## 7. 上游与模型资产

- LiveTalking 锁定版本见 `config/upstream-lock.json`。
- 模型权重和 Avatar 资产不进入 Git；仅记录来源、校验值、目标路径和许可状态。
- 不用空文件、重命名文件或伪造校验值冒充模型已下载。
- GPU、CUDA、PyTorch、FFmpeg 的兼容性必须用真实命令验证。

## 8. 代码与架构规则

- 外部服务必须经由 `ASRProvider`、`TTSProvider`、`LLMProvider`、`KnowledgeProvider` 等 Adapter；业务层不得直接写死供应商 SDK。
- Android 客户端只负责 UI、设备音频、状态和安全令牌；密钥与模型调用放在服务端。
- `idle_video` 与 `livetalking_webrtc` 必须可配置切换。
- 超时、有限重试、request_id、结构化日志和错误映射是正式功能的一部分。
- 先实现可观测、可回退的简单方案，再优化低延迟和视觉效果。

## 9. 测试与证据

每项“完成”至少包含：

- 实际执行命令；
- 退出码或测试结果；
- 关键日志路径；
- 生成物路径；
- 未覆盖项和环境限制。

设备、GPU 或收费 API 不可用时，要明确标记 `BLOCKED` 或 `PARTIAL`，不得用静态截图替代功能验证。

## 10. 文档更新

架构或边界改变时必须新增 ADR；不要悄悄覆盖既有决策。文档应使用明确日期、任务编号、版本和证据链接。项目记忆只保存稳定事实和可复用决定，不记录未经验证的猜测。

## 11. 停止条件

出现下列任一情况立即停止破坏性操作并报告：

- 需要删除或覆盖用户原文件；
- 发现真实密钥可能被追踪；
- 需要把敏感资料上传到外部服务；
- 上游许可证或人物/声音商用权不清楚；
- 本任务将不可避免地修改未授权范围；
- 测试会产生不可控费用或公网暴露。
